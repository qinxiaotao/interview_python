1.  # Python3 code to demonstrate conversion of lists to dictionary using naive method

```
    list1 = ['a','b','c']
    list2 = [1,2,3]
    c = dict(zip(list1,list2))
    print(c)

    list1 = ["a","b","c"]
    list2 = [1,2,3]
    c = { list1[i]:list2[i] for i in range(len(list1))}
    print(c)
```
